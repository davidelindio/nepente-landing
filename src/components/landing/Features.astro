---
import features from "../../config/features.json"
console.log(features)
---
<section id="works">
    <h1 class="text-center font-bold text-5xl mt-10">Características</h1>
<div class="grid md:grid-cols-3 gap-4 p-4">
    {
        features.map(feature => (
            <div class="bg-slate-800 p-10 rounded-xl">
                <h2 class="text-3xl font-bold my-4 text-wrap">{feature.title}</h2>
                <p class="text-2xl text-pretty">{feature.descripcion}</p>
            </div>

        ))
    }
</div>

</section>
